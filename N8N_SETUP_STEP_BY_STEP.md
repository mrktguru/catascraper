# 🔧 n8n - Пошаговая настройка (исправление ошибок)

## ❌ Проблема: "Column to Match On parameter is required"

Эта ошибка возникает из-за неправильной настройки Google Sheets node.

---

## ✅ РЕШЕНИЕ: Пошаговая настройка

### Шаг 1: Импортируйте ИСПРАВЛЕННЫЙ workflow

На сервере:
```bash
cat /root/cataparser/n8n_workflow_fixed.json
```

Скопируйте и импортируйте в n8n.

---

### Шаг 2: Настройте Google Sheets credentials

1. В n8n: **Settings** → **Credentials** → **New**
2. Выберите **Google Sheets OAuth2 API**
3. Заполните:
   - **Client ID**: из Google Cloud Console
   - **Client Secret**: из Google Cloud Console
4. Нажмите **Connect my account**
5. Авторизуйтесь в Google
6. Сохраните

**Подробная инструкция для Google Cloud Console:**

1. Откройте https://console.cloud.google.com
2. Создайте проект (или выберите существующий)
3. **APIs & Services** → **Enable APIs and Services**
4. Найдите и включите **Google Sheets API**
5. **Credentials** → **Create Credentials** → **OAuth client ID**
6. Application type: **Web application**
7. Authorized redirect URIs: скопируйте из n8n (показывается в окне создания credentials)
8. Скопируйте Client ID и Client Secret в n8n

---

### Шаг 3: Настройте Google Таблицу

**Создайте новую таблицу:**

1. https://sheets.google.com → **Blank**
2. Назовите: **Catawiki Scraper**

**Лист 1: URLs**

Переименуйте Sheet1 → **URLs**

Структура:
```
┌─────────────────────────────────────────────┐
│ A                                            │
├─────────────────────────────────────────────┤
│ url                                          │ <- Заголовок в A1
│ https://www.catawiki.com/en/l/98998534-...   │ <- Данные начиная с A2
│ https://www.catawiki.com/en/l/98998535-...   │
│ https://www.catawiki.com/en/l/98998536-...   │
└─────────────────────────────────────────────┘
```

**Лист 2: Results**

Создайте новый лист → **Results**

Заголовки в первой строке (A1:J1):
```
title | bottles_count | seller_name | current_price | shipping_cost | end_date | images_count | first_image | url | scraped_at
```

**Скопируйте ID таблицы:**

Из URL таблицы:
```
https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit
                                          ↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑↑
                                          Это ID - скопируйте его!
```

---

### Шаг 4: Настройте workflow в n8n

Откройте импортированный workflow.

#### **Нода 1: "Read URLs"**

1. Кликните на ноду **Read URLs**
2. **Credentials**: выберите ваши Google Sheets credentials
3. **Document**:
   - Нажмите на поле
   - Выберите **From list**
   - Найдите таблицу **Catawiki Scraper**
4. **Sheet**:
   - Выберите **URLs**
5. **Options** → **Range**: `A:A`

**Важно:** НЕ заполняйте "Column to Match On" - это для операции append, а не read!

#### **Нода 2: "Prepare URLs"**

Не требует настройки - JavaScript код уже настроен.

#### **Нода 3: "Start Batch Job"**

Проверьте URL:
- Если API на том же сервере: `http://localhost:8000/scrape-batch` ✅
- Если API на другом сервере: `http://IP_СЕРВЕРА:8000/scrape-batch`

#### **Нода 4: "Wait 5 Minutes"**

Таймаут по умолчанию: 5 минут.

Настройте в зависимости от количества URL:
- 1-10 URL = 3 минуты
- 10-50 URL = 10 минут
- 50-100 URL = 20 минут

#### **Нода 5: "Check Status"**

Не требует настройки.

#### **Нода 6: "Is Completed?"**

Не требует настройки.

#### **Нода 7: "Format Results"**

Не требует настройки - JavaScript код готов.

#### **Нода 8: "Write Results"**

1. **Credentials**: те же Google Sheets credentials
2. **Document**: таблица **Catawiki Scraper**
3. **Sheet**: выберите **Results**
4. **Columns**: уже настроены (10 колонок)

---

### Шаг 5: Тестовый запуск

1. Добавьте тестовый URL в Google Sheets:
   - Лист **URLs**
   - Ячейка **A2**: `https://www.catawiki.com/en/l/98998534-2022-beaune-1-cru-belissand-domaine-francoise-andre-burgundy-6-bottles-0-75l`

2. В n8n нажмите **Execute Workflow** (▶️ внизу)

3. Наблюдайте выполнение:
   - ✅ Read URLs
   - ✅ Prepare URLs
   - ✅ Start Batch Job
   - ⏳ Wait 5 Minutes (это займет 5 минут)
   - ✅ Check Status
   - ✅ Format Results
   - ✅ Write Results

4. Откройте Google Sheets → лист **Results**

5. Данные должны появиться! 🎉

---

## 🔍 Отладка ошибок

### Ошибка: "Column to Match On"

**Решение:**
- Используйте исправленный workflow: `n8n_workflow_fixed.json`
- В ноде "Read URLs" НЕ заполняйте "Column to Match On"
- Operation должна быть "Read", а не "Append"

### Ошибка: "Document not found"

**Решение:**
- Проверьте что вы авторизованы в Google Sheets credentials
- Убедитесь что таблица доступна для вашего Google аккаунта
- Попробуйте повторно выбрать таблицу из списка

### Ошибка: "API not responding"

**Решение:**
```bash
# На сервере проверьте API
curl http://localhost:8000/health

# Если не отвечает:
systemctl status catawiki-api
systemctl restart catawiki-api
```

### Ошибка: "Job not completed"

**Решение:**
- Увеличьте время в ноде "Wait"
- Проверьте статус job вручную:
```bash
curl http://localhost:8000/jobs
```

### Workflow не запускается по расписанию

**Решение:**
- Убедитесь что workflow **активирован** (переключатель ON)
- Проверьте cron выражение в Schedule Trigger
- Проверьте timezone в n8n settings

---

## 📊 Альтернатива: Ручная настройка node

Если импорт не работает, создайте workflow вручную:

### Создайте новый workflow

1. **Schedule Trigger**
   - Cron: `0 9 * * *`

2. **Google Sheets (Read)**
   - Operation: **Read**
   - Document: Ваша таблица
   - Sheet: URLs
   - Options → Range: A:A

3. **Code Node**
   - Mode: Run Once for All Items
   - JavaScript:
   ```javascript
   const urls = $input.all()
     .map(item => item.json.A)
     .filter(url => url && url.startsWith('http'));

   return { json: { urls: urls } };
   ```

4. **HTTP Request (Start Batch)**
   - Method: POST
   - URL: `http://localhost:8000/scrape-batch`
   - Body:
   ```json
   {
     "urls": {{ $json.urls }},
     "headless": true,
     "save_csv": true
   }
   ```

5. **Wait**
   - 5 minutes

6. **HTTP Request (Check Status)**
   - Method: GET
   - URL: `http://localhost:8000/job/{{ $json.job_id }}`

7. **IF (Is Completed?)**
   - Condition: `{{ $json.status }}` equals `completed`

8. **Code Node (Format)**
   - JavaScript: (см. в workflow)

9. **Google Sheets (Write)**
   - Operation: **Append**
   - Document: Ваша таблица
   - Sheet: Results
   - Columns: Define Below
     - title: `{{ $json.title }}`
     - bottles_count: `{{ $json.bottles_count }}`
     - и т.д.

---

## ✅ Проверочный список

Перед запуском убедитесь:

- [ ] API сервер запущен: `curl http://localhost:8000/health`
- [ ] Google Sheets credentials настроены и работают
- [ ] Таблица создана с листами URLs и Results
- [ ] Заголовки в Results совпадают с настройками workflow
- [ ] Есть хотя бы один URL в листе URLs (начиная с A2)
- [ ] Workflow использует правильную версию (fixed)
- [ ] В ноде "Read URLs" НЕТ "Column to Match On"
- [ ] Все ноды подключены к правильным credentials

---

## 🎯 Быстрое тестирование

Минимальный тест без n8n:

```bash
# 1. Тест API
curl http://localhost:8000/health

# 2. Тест парсинга напрямую
python3 scraper_pro.py "https://www.catawiki.com/en/l/98998534-2022-beaune-1-cru-belissand-domaine-francoise-andre-burgundy-6-bottles-0-75l" --headless --csv

# 3. Проверить результат
cat catawiki_data.csv
```

Если это работает, значит проблема только в настройках n8n.

---

## 📞 Дополнительная помощь

Если проблема остается:

1. Покажите скриншот ноды "Read URLs" с настройками
2. Покажите полный текст ошибки из n8n
3. Проверьте логи API: `tail -f /var/log/catawiki-api.log`

---

## 🎉 После успешного запуска

1. Активируйте workflow (переключатель ON)
2. Настройте расписание в Schedule Trigger
3. Добавляйте URL в Google Sheets
4. Получайте результаты автоматически!

Готово! 🚀
